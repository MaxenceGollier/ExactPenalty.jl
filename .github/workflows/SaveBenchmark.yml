name: Save benchmarks on main

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "**/*.toml"

  schedule:
    - cron: "0 0 1 */2 *"

  workflow_dispatch:

jobs:
  run-benchmarks:
    uses: ./.github/workflows/RunBenchmark.yml
